import { DisabledAdapter } from "./disabled-adapter.js";

export class OpencodeAdapter extends DisabledAdapter {
  constructor() {
    super("opencode", "opencode", "opencode provider arrives after Antigravity (Phase 2).");
  }
}
